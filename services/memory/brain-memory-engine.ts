export interface MemoryRecord{id:string;type:string;subject:string;content:Record<string,unknown>;tags:string[];createdAt:string;updatedAt:string;confidence:number;} export interface MemoryStore{put(record:MemoryRecord):Promise<void>;get(id:string):Promise<MemoryRecord|null>;search(query:string):Promise<MemoryRecord[]>;} export class BrainMemoryEngine{constructor(private store:MemoryStore){} async remember(record:MemoryRecord){record.updatedAt=new Date().toISOString();await