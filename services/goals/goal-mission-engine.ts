export interface Goal{id:string;title:string;priority:number;status:'planned'|'active'|'completed';missions:string[]} export interface Mission{id:string;goalId:string;title:string;status:'pending'|'running'|'completed';dependencies:string[]} export class GoalMissionEngine{private goals=new Map<string,Goal>();createGoal(goal:Goal){this.goals.set(goal.id,goal);return goal;}prioritize(){return [...this.goals.values()].sort((a,b)=>b.priority-a.priority);}completeGoal(id:string){const g=this.goals.get(id);if(g)g.status='completed';return g;}}