VITE_CHARACTER_DATA_SOURCE=api
VITE_NEXKOSMO_API_BASE_URL=/api/v1
VITE_NEXKOSMO_DEV_PROXY_TARGET=http://127.0.0.1:8000

# Authentication is supplied at runtime by the future identity/session adapter.
# Never place an access token in Vite environment files: VITE_* is public.
